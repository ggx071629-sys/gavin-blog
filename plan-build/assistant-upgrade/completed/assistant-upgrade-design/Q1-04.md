# Q1-04：SDD 与适用验证映射

2026-09-11；状态：完成。两份原始 spec 已在 Git 提交 `bab867f`，提交后分别通过 `spec-lint`。首次提交前检查因不在 HEAD 被拒绝，已按规则补齐先提交前置后重跑通过；没有运行产品测试。

## 实施规格

通过 [规格索引](../../../../harness/specs/INDEX.md) 查任务 `20260911-assistant-about-source`（Q2-01—Q2-05）与 `20260911-assistant-resume-source`（Q3-01—Q3-07）。两份 spec 保持 active，完成产品验证后只能通过 Harness close 关闭；本设计任务归档不关闭它们。

## 验证映射

以下为计划 case，不是已存在或已通过测试的声明。实现时必须登记 module_coverage 合同中的 exact refs、required/forbidden expectations 和 ownership/逐文件 impact；未知或缺失映射阻止正式 verify。

| 范围 | Case | 适用验证 |
| --- | --- | --- |
| Q2-01 | M11-ASSISTANT-PROJECTION-01 | API：资格、结构投影、草稿/seed 排除、数据库旧数据升级 |
| Q2-02 | M11-ASSISTANT-SYNC-01 | API：发布/回滚原子入队、失败回滚、旧证据和迟到任务失效 |
| Q2-03 | M09-ABOUT-RETRIEVAL-01 | API：初次重建、关键词/向量混合检索、证据预算及版本复检 |
| Q2-04 | M09-ABOUT-CITATION-01、M09-ABOUT-FACTS-01 | E2E：公开/管理试问引用跳转；API：自述/主题区分和冲突提示规则 |
| Q3-01 | M06-RESUME-DOWNLOAD-01 | API：可信地址、DNS/IP/重定向、流式限额、总截止、错误脱敏 |
| Q3-02 | M06-RESUME-PARSE-01 | API：文本 PDF/扫描/加密/混合失败，真实解析子进程资源强制边界 |
| Q3-03 | M06-RESUME-VERSION-01 | API：绑定代次、hash 复用、原子替换/清理、迁移备份、迟到结果 |
| Q3-04 | M10-RESUME-REFRESH-01 | API：首次/换址/手动请求、合并、有限重试、无周期和时间过期 |
| Q3-05 | M06-RESUME-ADMIN-01 | API + E2E：真实状态、Session/CSRF/409/429、双主题手机键盘 |
| Q3-06 | M09-RESUME-EVIDENCE-01、M09-RESUME-CITATION-01 | API + E2E：预算、不可用来源、冲突、版本 PDF 下载与撤销 |
| Q3-07 | M09-RESUME-FLOW-01 | E2E：隔离导入/更新/失败/撤销闭环；真实文件条件独立记录 |

Q2-05 和 Q3-07 汇总对应已选 case，不机械增加全量发布测试。涉及迁移/OpenAPI/消费类型的变更必须在相应精确 case 中检查实际合同；包门禁不独占多个 AC。

## 文档影响与验证输入

- 关于页 spec：API/Web/Contracts README 必须在 spec 首次提交后更新并提交。
- 简历 spec：另更新 architecture/boundaries，说明短生命周期解析子进程、内容库 PDF 和备份所有权；不新建部署目录。
- 新模块无需新增编号，复用 M06/M09/M10/M11；具体 case 在其现有 authority 下登记。
- 影响计划分别使用 `harness/verification/impact/<task_id>.json`；所有权以已有 ownership.json 为基础，逐个审查可达消费者；测试只能从提交的完整输入快照执行。
- 新增 durable 决定进入 `harness/docs/decisions/`，索引元数据变更后重生成；根计划是过程导航，不代替运行边界。
- 用户原有 Web 与 package 改动保持未暂存；此次 spec 提交只有两份 spec 和生成索引。

## 验收

两次提交后 spec-lint 均退出 0；11 条 AC 均有对应 case 与 leaf gate；设计约束、文档影响、阶段映射和真实输入缺失均明确。下一项阶段二 Q2-01。
