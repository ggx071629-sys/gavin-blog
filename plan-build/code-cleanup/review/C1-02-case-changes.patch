--- a/harness/verification/checks/module_coverage.contract.json
+++ b/harness/verification/checks/module_coverage.contract.json
@@ -736,41 +736,30 @@
         {
           "id": "M01-UNIT-ARTICLE-01",
           "priority": "P1",
-          "scenario": "M01.reading: article scenarios at the existing module boundary.",
+          "scenario": "Existing production slug normalization and create-field validation remain unchanged after unused route/filter helpers are removed.",
           "expectations": [
             {
               "id": "M01-UNIT-ARTICLE-01-E01",
               "surface": "response",
-              "statement": "builds a stable UTC publication path; creates URL-safe ASCII slugs; builds public taxonomy filters from supported query values"
+              "statement": "ASCII slug normalization remains stable; a valid title and slug produce no field errors."
             },
             {
               "id": "M01-UNIT-ARTICLE-01-E02",
               "surface": "forbidden",
-              "statement": "requires an explicit English or pinyin slug when the title cannot generate one; rejects empty titles and slugs outside the API slug contract"
+              "statement": "A title that cannot generate an ASCII slug requires an explicit slug; empty titles and invalid slugs remain rejected."
             }
           ],
           "required_gates": [
             "web-quality"
           ],
           "test_refs": [
-            {
-              "kind": "vitest",
-              "path": "apps/web/tests/unit/article.test.ts",
-              "name": "builds a stable UTC publication path",
-              "gate": "web-quality",
-              "covers": [
-                "M01-UNIT-ARTICLE-01-E01",
-                "M01-UNIT-ARTICLE-01-E02"
-              ]
-            },
             {
               "kind": "vitest",
               "path": "apps/web/tests/unit/article.test.ts",
               "name": "creates URL-safe ASCII slugs",
               "gate": "web-quality",
               "covers": [
-                "M01-UNIT-ARTICLE-01-E01",
-                "M01-UNIT-ARTICLE-01-E02"
+                "M01-UNIT-ARTICLE-01-E01"
               ]
             },
             {
@@ -779,7 +768,6 @@
               "name": "requires an explicit English or pinyin slug when the title cannot generate one",
               "gate": "web-quality",
               "covers": [
-                "M01-UNIT-ARTICLE-01-E01",
                 "M01-UNIT-ARTICLE-01-E02"
               ]
             },
@@ -787,16 +775,6 @@
               "kind": "vitest",
               "path": "apps/web/tests/unit/article.test.ts",
               "name": "rejects empty titles and slugs outside the API slug contract",
-              "gate": "web-quality",
-              "covers": [
-                "M01-UNIT-ARTICLE-01-E01",
-                "M01-UNIT-ARTICLE-01-E02"
-              ]
-            },
-            {
-              "kind": "vitest",
-              "path": "apps/web/tests/unit/article.test.ts",
-              "name": "builds public taxonomy filters from supported query values",
               "gate": "web-quality",
               "covers": [
                 "M01-UNIT-ARTICLE-01-E01",
@@ -12524,11 +12502,10 @@
             {
               "kind": "vitest",
               "path": "apps/web/tests/unit/assistant.test.ts",
-              "name": "counts unicode code points and wipes on BFCache",
-              "gate": "web-quality",
-              "covers": [
-                "M09-UI-BOUNDARY-01-ALLOW",
-                "M09-UI-BOUNDARY-01-DENY"
+              "name": "counts unicode code points and initializes empty memory",
+              "gate": "web-quality",
+              "covers": [
+                "M09-UI-BOUNDARY-01-ALLOW"
               ]
             }
           ]
--- a/apps/web/tests/unit/article.test.ts
+++ b/apps/web/tests/unit/article.test.ts
@@ -1,17 +1,11 @@
 import { describe, expect, it } from 'vitest'
 
 import {
-  articleFilterQuery,
-  articlePath,
   slugify,
   validateArticleCreateFields,
 } from '../../utils/article'
 
 describe('article utilities', () => {
-  it('builds a stable UTC publication path', () => {
-    expect(articlePath('2026-07-31T23:30:00Z', 'harness-engineering'))
-      .toBe('/notes/2026/07/harness-engineering')
-  })
 
   it('creates URL-safe ASCII slugs', () => {
     expect(slugify(' Reliable  Agent Harness! ')).toBe('reliable-agent-harness')
@@ -35,9 +29,4 @@
     })
   })
 
-  it('builds public taxonomy filters from supported query values', () => {
-    expect(articleFilterQuery({ category: 'engineering', tag: ['fastapi', 'ignored'] }))
-      .toBe('?category=engineering&tag=fastapi')
-    expect(articleFilterQuery({ category: undefined, page: '2' })).toBe('')
-  })
 })
--- a/apps/web/tests/unit/assistant.test.ts
+++ b/apps/web/tests/unit/assistant.test.ts
@@ -6,17 +6,15 @@
   ASSISTANT_MAX_EVENTS,
   AssistantSseError,
   AssistantSseParser,
-  splitSseForTest,
 } from '../../utils/assistant/sse'
+import { splitSseForTest } from '../helpers/assistant-sse'
 import { readFileSync } from 'node:fs'
 import { resolve } from 'node:path'
 import { closeAnswerCitations, splitAnswerMarkers } from '../../utils/assistant/citations'
 import { mapAssistantCode, parseRetryAfter } from '../../utils/assistant/errors'
 import {
-  assistantStorageIsClean,
   COUNT_POINTS,
   emptyAssistantMemory,
-  shouldWipeOnPageShow,
 } from '../../utils/assistant/persistence'
 import { assertResponseUrl, parseSessionStatus } from '../../utils/assistant/client'
 import { AssistantGenerationFence } from '../../utils/assistant/generation'
@@ -305,13 +303,8 @@
     expect(unknown.action).toBe('refresh')
   })
 
-  it('counts unicode code points and wipes on BFCache', () => {
+  it('counts unicode code points and initializes empty memory', () => {
     expect(COUNT_POINTS('你好a')).toBe(3)
-    expect(shouldWipeOnPageShow(true)).toBe(true)
-    expect(shouldWipeOnPageShow(false)).toBe(false)
     expect(emptyAssistantMemory().idempotencyKey).toBeNull()
-    expect(assistantStorageIsClean(localStorage)).toBe(true)
-    localStorage.setItem('theme', 'dark')
-    expect(assistantStorageIsClean(localStorage)).toBe(true)
-  })
-})
+  })
+})
--- a/apps/web/tests/helpers/assistant-sse.ts
+++ b/apps/web/tests/helpers/assistant-sse.ts
@@ -0,0 +1,13 @@
+export function splitSseForTest(text: string, sizes: number[]): Uint8Array[] {
+  const bytes = new TextEncoder().encode(text)
+  const chunks: Uint8Array[] = []
+  let offset = 0
+  let index = 0
+  while (offset < bytes.length) {
+    const size = sizes[index % sizes.length] ?? bytes.length
+    chunks.push(bytes.slice(offset, offset + size))
+    offset += size
+    index += 1
+  }
+  return chunks
+}
