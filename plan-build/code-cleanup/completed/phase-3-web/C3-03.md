# C3-03：移除未使用 React GSAP 适配依赖

2026-09-11 完成。

npm uninstall --offline --ignore-scripts --no-audit --no-fund 成功，仅删除 @gsap/react 与无剩余消费者的 React peer 两个包；锁文件没有新增或升级，只有根依赖及两条包记录删除。npm ls 确认 root/Web 共用 gsap 3.15.0，两处声明与 GSAP 锁记录完全不变。Harness 10 项结构检查通过；动效浏览器验证留在本阶段收尾与最终精确验证共同执行。

下一项：C3-04。
