# C4-03：真实启动、资料读取与 GSAP

2026-09-11 完成既定启动验收，保留既有检索限制。

用户明确授权读取本机配置并启动真实项目。既有快捷入口启动 Web/API/独立 worker/E5/Qdrant；公开接口同源代理与直连一致，available=true，worker 心跳推进，真实 E5/Qdrant 检索读取当前有效版本。原有 GSAP 浏览器用例直接指向真实站点通过，无 API mock。

当前有效来源为 3 篇文章、个人资料及简历，25 个切片与向量；PDF 下载及哈希核对通过。启动前备份两库，停止后逐表行摘要核对：内容、索引、任务和费用不变，仅心跳、观测指标与 runtime 控制状态正常更新。仅停止本轮实例，五个监听端口均已释放。

证据：[启动核对](../../review/C4-STARTUP.md)、[紧凑 manifest](../../review/C4-REAL.json)。本机原始资料位于 `.run/cleanup/real-c4/`，不提交备份、密钥、日志或截图。

既有第 14 代 FTS 仅 9/25、历史第 15 代待切换与两条失败任务保持原样，详见启动核对中的 L-C4-01/02。没有重建或切换索引，没有发送付费 Chat；本结果不代表完整检索质量或生产资格。下一项：C4-04 的正式验证与确定性关闭。
