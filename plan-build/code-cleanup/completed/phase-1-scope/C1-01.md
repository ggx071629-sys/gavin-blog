# C1-01：候选、使用路径与保留边界

日期：2026-09-11。范围：完成 C1-01 复核；下表“删除”表示交接给后续实施项的处置决定，本项没有删除产品源码或依赖，也没有执行修复后产品验证。

## 过程与依据

- 开始时 `git status --short` 为 `M plan-build/README.md`、`?? plan-build/code-cleanup/`；均为既有计划工作，不覆盖、不代为提交。源码、依赖与锁文件无已跟踪改动。
- 按阶段二、三列名搜索 `apps`、`packages`、根 package/lock 与 `scripts`，读取 Python AST 定义、函数体、实际路由、Worker、CLI、测试、Vue 模板及 Nuxt 自动导入声明。未发现候选名的动态分发；这只证明仓库内已检查路径，不证明没有仓库外维护消费者。
- Nuxt 生成的 imports 声明包含 articlePath、articleFilterQuery、parseAssistantTasks；自动注册不等于实际使用，业务源码和模板没有调用它们。生成声明由后续构建更新，不手工修改。
- `npm ls @gsap/react gsap react --all --offline` 成功：根 @gsap/react 2.1.2 → gsap 3.15.0 与 peer react 19.2.8；根和 Web 的 gsap 共用安装实例。锁文件只在根声明 @gsap/react，没有其他依赖消费者。未执行安装或卸载。

## 后端逐项处置

路径均相对 `apps/api/app/`；16 个函数及一个专用类均已复核。删除前仍须按实施项重查漂移。

| 候选位置 | 处置 / 实施项 | 依据及必须保留的行为 |
| --- | --- | --- |
| assistant/store.py:384 public_completed_turn | 删除 / C2-01 | 无调用；session_view 实际调用 public_completed_turn_at，保留后者的到期序列化。 |
| assistant/store.py:1195 clear_transient | 删除 / C2-01 | 无调用的 parsed_json/vector_json 更新封装；保留现有清理链与账本。 |
| assistant/store.py:1215 count_live_subscribers | 删除 / C2-01 | 无调用且固定返回 0；实际订阅计数、IP/turn 限制在 assistant/hub.py 的 EventHub。 |
| assistant/store.py:1298 estimate_bytes | 删除 / C2-01 | 无调用的 UTF-8 长度求和；不改变实际存储预算。 |
| assistant/readiness.py:259 _Gen、:271 _generation_stub | 删除 / C2-02 | _Gen 只被无消费者的 stub 使用；保留 main、write_receipt 与真实资格绑定校验。 |
| assistant/hydrate.py:146 recheck_evidence | 删除 / C2-02 | 无调用；保留 hydrate_descriptors 与实际证据投影/版本校验链。 |
| assistant/money.py:16 micro_to_cny | 删除 / C2-02 | 无调用的金额显示转换；整数预算与 Decimal 的其他使用不受影响。 |
| assistant/pragmas.py:65 begin_immediate | 删除 / C2-02 | 无调用封装；不删实际控制连接的 immediate 事务及 PRAGMA 配置。 |
| assistant/routes.py:176 _cache_headers | 删除 / C2-02 | 无调用封装；保留现有响应头设置与 SSE 链。 |
| assistant/runner.py:192 _lease_token | 删除 / C2-02 | 无调用查询；保留实际 fencing_token、租约所有者校验与 worker_control.new_lease_token。 |
| assistant/runtime_schema.py:39 index_names | 删除 / C2-02 | 无调用；历史迁移中的 _index_names 有调用，不能连带删除。 |
| assistant/runtime_schema.py:107 runtime_file_paths | 删除 / C2-02 | 无调用路径字典；不操作 DB/WAL/SHM/owner lock 实体。 |
| assistant_index/worker.py:696 _abandon_generation | 删除 / C2-03 | 无调用的私有旧删除链；不执行其中的向量/FTS/记录删除，不改现行 Worker 清理策略。 |
| assistant_index/worker.py:710 switch_generation | 保留 / C2-03 | 虽无仓库调用，但明确拒绝绕过 API-owner finalize，属于防误用边界；不按机械死代码移除。 |
| routes/admin_content.py:145 trash_item | 删除 / C2-03 | 无调用；list_trash 与 restore 直接构造 TrashItem，保留 schema、路由和真实构造链。 |
| search.py:177 rebuild_search_index | 保留 / C2-03 | 无仓库调用或已发现 CLI 接线，但可手工重建普通搜索 FTS；无法排除运维使用，不以零引用判无用，也不运行它。 |

额外保留：两套迁移链、incubator 历史兼容值、assistant_runtime_migrations/env.py → runtime_models.RuntimeBase 的 ORM 元数据注册。readiness、Worker 及实际维护命令入口未改。

## Web、依赖与产物逐项处置

路径除根依赖外均相对 `apps/web/`。

| 候选 | 处置 / 实施项 | 使用路径与理由 |
| --- | --- | --- |
| utils/assistant/origin.ts:assistantApiUrl | 删除 / C3-01 | 无显式或模板调用；保留同源校验等同文件有效逻辑。 |
| utils/assistant/errors.ts:AssistantUiState | 删除 / C3-01 | 未消费类型；错误文案和实际状态处理保留。 |
| types/api.ts:AboutRevisionListResponse | 删除手写类型 / C3-01 | Web 无消费；API schema 与 Contracts 生成同名类型仍有效。 |
| types/api.ts:ApiErrorBody | 删除手写类型 / C3-01 | Web 无消费；生成 ApiErrorEnvelope 仍引用同名生成类型。 |
| types/api.ts:ArticleRevisionListResponse | 删除手写类型 / C3-01 | Web 无消费；文章修订接口的生成响应仍有效。 |
| types/api.ts:ArticleRollbackRequest | 删除手写类型 / C3-01 | Web 无消费；回滚接口的生成请求仍有效。 |
| utils/article.ts:articlePath | 删除及专用自证断言 / C3-02 | 只有 article.test 调用；ArticleCard 用后端 public_path，不重算年月路由。 |
| utils/article.ts:articleFilterQuery | 删除及专用自证断言 / C3-02 | 只有 article.test 调用；articles/index.vue 实际读取 route.query，链接使用 Vue query 对象。 |
| utils/assistant-admin.ts:parseAssistantTasks | 保留 / C3-02 | 只有 assistant-admin.test 调用；旧 /index/tasks API 仍存在。useAssistantManagement 实际请求 /sync，SyncTask 多出 title/public_path/current_revision/provider_state，旧 parser 的 exact 会拒绝这些字段。不能直接接入；现行列表缺少运行时 parser 属独立行为完善，不纳入机械清理。 |
| utils/assistant/persistence.ts:assistantStorageIsClean | 删除及专用自证断言 / C3-02 | 仅测试调用，按 assistant 子串禁止所有 key，与现行允许的悬浮球偏好不相容；不是有效的生产隐私防线。保留问题/回答不落盘的现行回归覆盖。 |
| utils/assistant/persistence.ts:shouldWipeOnPageShow | 删除及专用自证断言 / C3-02 | 仅返回 persisted；AssistantHost 的真实 pageshow 已直接判 event.persisted，保留 Host 和 panel.spec 的恢复覆盖。 |
| utils/assistant/sse.ts:splitSseForTest | 迁入测试辅助 / C3-02 | assistant.test 的分片解析用例实际使用，迁移 import 并保留完整分片断言；不删生产 SSE parser。 |
| 根 @gsap/react | 删除直接依赖并重算锁 / C3-03 | Vue 项目无源码消费者，唯一引入路径为根依赖；react 当前为该包 peer。锁变更若涉及 React，仍重核剩余消费者，不手工删传递锁条目。 |
| 根与 Web 两处 gsap | 保留 / C3-03 | AssistantDiscovery.vue:184 动态加载 orb-motion.ts → gsap/ScrollTrigger；安装去重不等于声明可以删除。保留 orb-motion/discovery 回归。 |
| public/og/gavin-notes-default.svg | 保留 / C3-04 | useSiteSeo 与文章 SEO 指向 PNG，但公开 SVG 可能被存储内容/外链直接使用；本项未读取真实内容库，不能证明无消费者。PNG 与生成契约不在删除范围。 |
| 127.0.0.1/ | 待查，默认保留 / C3-04 | 实际绝对路径 D:/Project/my_blog/apps/web/127.0.0.1，已忽略，含 .nuxt/{dev,dist,manifest,schema,types} 与 node_modules。检查的目录本身未显示链接属性；未核实递归链接和进程占用，不能推断整个目录可删。后续删除前核验所有最终目标、归属与占用。 |

## 运行模式与数据归属

下列为源码/命令配置事实，不是当前服务存活或本机配置值的声明；没有读取密钥值、启动服务、迁移真实库或触发模型请求。

| 模式 | 配置及端口 | 数据归属 |
| --- | --- | --- |
| 普通开发 | apps/api/app/config.py 读取 apps/api/.env；API 默认 8000、Web 默认 3000 | API 内容 SQLite 默认 apps/api/data/gavin.db、媒体默认 apps/api/data/media；配置可覆盖，不能当作真实问答内容库。 |
| 离线问答 | scripts/run-assistant-dev.mjs → apps/api/scripts/run_assistant_dev.py；.env；Web 3101、API 8101，可由 GAVIN_ASSISTANT_DEV_WEB_PORT/API_PORT 覆盖 | 读取配置中的现有内容库；短会话 runtime 在系统临时 gavin-assistant-dev-* 目录，向量/供应商为受控离线实现。启动会迁移内容库，本次不启动。 |
| 真实问答 | 同脚本 --real；.env.e5 作为 base、.env 作为 Chat Settings；默认 Web 3101/API 8101 | 内容库/媒体与 E5/Qdrant 配置来自真实模式合成 Settings；runtime 固定根为 apps/api/data/assistant-local-real。启动器默认 E5 8091、Qdrant 8092；向量为派生数据，Worker 不打开短会话库。 |
| 测试与契约生成 | apps/api/scripts/run_assistant_e2e.py、apps/web/scripts/run-playwright-e2e.mjs 和各 Playwright config；contracts/generate.mjs → API export_openapi.py | 使用隔离测试数据；契约导出使用 test Settings、内存库、_env_file=None。准确测试端口与 exact refs 由 C1-02 的所选检查确定，不能复用真实问答库。 |

## 验证与交接

- 本项检查：源码/测试/动态与自动注册路径审查、Python AST 枚举、离线依赖树解析成功。静态检索发现已逐项归属，不将未发现调用冒充运行时无消费者证明。
- 文档影响：仅非执行阶段计划、入口和本归档；无 Harness authority/合同/索引元数据变化，不需新 SDD 或重新生成 Harness 索引。
- 本项完成前检查阶段状态、归档链接、候选覆盖及源码未改。没有运行 Ruff/Pytest/Vitest/Playwright/构建；本项通过只代表 C1-01 审查通过，修复后验证尚未发生。
- C1-02 必须建立依赖 SDD 与独立影响计划，选精确 case/ref，审查根依赖锁的传递影响；C1-03 再运行选定基线。禁止用全量 release 替代范围分析。
- 保留决定交接：switch_generation、rebuild_search_index、parseAssistantTasks、SVG；产物占用与递归路径安全交给 C3-04，未决期间不删，不阻塞本项复核完成。

返回：[阶段一当前任务](../../PHASE-1-SCOPE.md#当前任务)。
