# Q5 题集与真实链路参考

本文件只保留已准备的题集和真实链路参考，不增加材料任务。实体手机和人工读屏已按用户要求取消，不是通过，不再作为待办或阻塞。目标是修复问答助手并跑通真实链路；Q6继续取消。

当前真实页面链路与零调用刷新恢复已完成，见[最终记录](Q5-live-results.json)；下列步骤仅为可复核材料，旧空白结果不代表链路仍待执行。独立人工全文、标注和歧义复核仍未验证，不新增人工验收阶段。

## 题集全文复核

来源：[固定清单](Q5-corpus.json)；旧题：[Q5-cases.json](Q5-cases.json)；新题：[Q5-holdout.json](Q5-holdout.json)。核对当前发布版本及正文SHA，全文以本机已授权公开投影为准，不能只看检索片段。公开入口由清单定位；简历项目、角色和能力只作为自述，XX公司/日期/联系方式是占位符。

1. 复核者先读来源与题目，不看模型输出，逐题确认可答性、完整期望和需要保留的限定。记录身份/日期以及是否参与调试。
2. E01“正文从哪句话开始”按首个叙述句“从这里开始写作。”判定；“# 新文章”属于正文中的标题。复核者应注明题意歧义是否需要改写，保留原题和首次结果。
3. 数值和主体逐项对应，不以关键词共现替代关系；表格要核对字段及完整解释，简历要核对项目归属、页码、全部限定和句尾。
4. H01—H08及追问首次结果已冻结保留，后续输出仅作已见题回归；D02是旧校准题，永远排除在新题结果外。新题由同一助手选取，不称独立人工抽样。
5. 输出生成后另表复核正确性、问题覆盖、语言、引用、误拒、追问。能被校验器接受不等于正确；没有输出不能判为误拒。

| 题号 | 复核者/日期 | 完整期望及限定 | 歧义/建议 | 来源版本/页码 | 正确性 | 语言 | 引用 | 误拒理由 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 每个D/E/H基础题及追问各填一行 | 待填写 | 待填写 | 待填写 | 待填写 | 未验证 | 未验证 | 未验证 | 未验证 | 未验证 |

判定使用通过/失败/部分/未验证并写理由，不强制压成单一分数。误拒分母只含已实际送模型且人工确认有完整依据的问题，正确弃答和传输未知单列。

## 真实页面完整链路

本次访问 `http://127.0.0.1:3101` 得到 `ERR_CONNECTION_REFUSED`。未知账本记录同时阻止新的付费调用；没有发送页面问题，也没有运行替身冒充真实链路。

恢复执行时先核对同一公开来源、配置和原累计账本。当前真实开发启动器的在线预算不能自动当作评测签名账本；每次真实provider dispatch（包括内部第二次生成）均必须在同一签名账本预留/结算。不通过单独启动器绕开累计限制，不创建第二份授权。未知调用未按原恢复流程处理之前不运行此项。

1. 在本站SSL文章页打开助手，发送旧题E14“配置SSL文中443端口是什么用途？”。保留本机request/turn ID与模型attempt ID对应关系，不记录Cookie、密钥或完整网络日志到Git。
2. 核对本地检索→实际模型usage/model/finish→应用校验→HTTP SSE终态；回答及引用与当前文章一致。模型原输出被拒绝时如实记失败，不能替换回答。
3. 发送E14真实追问“它还提到了什么自动续期测试命令？”，验证前序成功来源驱动resolver，得到续期命令及当前引用。
4. 刷新页面，核对同一会话两轮问题、答复、引用和状态恢复；通过账本确认刷新新增Chat为0。
5. 将旧题E15作为实际第三轮换题，确认不继承SSL来源。现有命令行运行器的`after`字段未用于持久会话，不能以其单题结果声称换题链路通过。

| 环节 | 证据字段 | 当前结果 |
| --- | --- | --- |
| 页面提问/请求准入 | 本机request ID、turn ID、时间 | 未验证 |
| 实际模型 | 同一签名账本attempt ID、response model、usage、finish | 未验证 |
| 校验/引用 | 终态码、逐引用来源版本/标题/章节或页码 | 未验证 |
| SSE | 同一turn关联的实际事件序列和最终状态 | 未验证 |
| 追问/换题 | 前序成功来源、追问及换题独立结果 | 未验证 |
| 刷新恢复 | 刷新前后会话结果一致且Chat增量为0 | 未验证 |
| 时延与费用 | 分开记录调用、首事件/终态、总链路；未知保留null | 未验证 |

## 未知调用处理交接

未知attempt：`fbd93334-dc25-4a77-a7f5-5f8c2f869558`。运行进程已退出，保守记账54291 micro-CNY，不表述为供应商实际收费；原状态和费用不得删除、回滚或重置。恢复入口是现有 `local_ledger.recover` / provider qualification CLI，需要确认静默并记录理由；本次没有执行恢复或自动重试。连接错误只记录到`OpenAIConnectionError`，未保存更深异常链，不能据此断言供应商未收到请求。

本地E5/Qdrant准备也发生dense降级，先诊断服务可达性。新题只读准备成功不证明dense检索或真实质量通过。恢复后使用 `--dataset holdout` 与 `--dataset regression` 分开保存首次新题评估及已见题回归，按实际提示逐次预留，剩余额度不是必须耗尽的目标。
