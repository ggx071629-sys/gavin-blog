# Q1-02：修正竞态与旧库恢复测试前置条件（G16）

- 状态：已验收并归档。
- 规格先行提交：`6367cf1`；实现提交：`45ae594`。
- 规格归档：[20260912-assistant-regression-prerequisites](../../../../harness/specs/archive/20260912-assistant-regression-prerequisites.md)
- 机器证据：[20260912-assistant-regression-prerequisites.json](../../../../harness/verification/evidence/20260912-assistant-regression-prerequisites.json)

## 修复内容

三个失效用例都保留了原断言，只修正它们的前置条件：

| 用例 | 原前置 | 现前置 |
| --- | --- | --- |
| `test_late_provider_completion_after_delete_has_no_body_checkpoint_or_delivery` | 输入预算 4000 | 8000，并断言模型替身确实被调用 |
| `test_normal_mutation_permit_requires_exact_lease_scope_keys` | 输入预算 4000 | 8000，并断言模型替身确实被调用 |
| `test_restore_previous_schema_creates_shared_budget_and_keeps_restore_lock` | 从最新 schema 删两张预算表并回写版本号 | Alembic 迁移到 `20260907_0024` 生成真实旧结构 |

- 两个竞态用例的失败原因是固定提示与协议开销已超过 4000，请求在模型调用前就被预算拒绝，竞态路径从未执行。断言改为带说明的失败，前置再次漂移时会直接指出原因，而不是进入后续断言。
- 旧库用例现在先在快照前断言版本号为 `20260907_0024`、共享预算表与 `admin_credentials` 均不存在，再断言快照清单记录同一版本；原来的合成降级会保留当时并不存在的表，导致迁移重复建表。
- 恢复断言未改动：迁移到 head、写入恢复日预算锁、预留表为空。

## 验收

- `python -m tools.harness verify 20260912-assistant-regression-prerequisites --close`
- 范围：2 cases / 8 exact refs；`api-tests` 与 `harness-integrity` 通过，隔离 worktree 清理与确定性关闭成功。
- 新增合同 case `M10-UNIT-ASSISTANT-LEGACY-SCHEMA-RESTORE-01`，使旧库恢复用例第一次进入模块验证覆盖（此前它不在任何 case 的 test_refs 中）。

## 边界与残余

- 只改测试前置与旧库构造，产品运行路径、提示词、预算上限与迁移代码未改。
- “独立诊断通过”与“原测试已修复”已分开：本轮结论来自原测试本身在目标前置下重新执行。
- 旧库断言依赖迁移 revision 名称 `20260907_0024`；若后续压缩或重命名迁移，需要同步复核该前提。
