# Q1-05 集成回归与阶段收尾

- 结果：当前集成输入 `8e12b98` 下 44 项通过，0 失败、错误、跳过；[紧凑证据](Q1-05-evidence.json)。
- 执行范围：`test_assistant_index.py`、`test_index_integrity.py`、`test_search.py`，以及原来的删除迟到响应、精确 lease scope、真实旧 schema 恢复三项回归。原始 JUnit 留在本机 `apps/api/data/qa-gap-closure-q1-05.xml`，未提交。
- Q1-04 的正式证据已覆盖 9 个精确引用，包括普通/E5 最终 MATCH、token/字符预算、超长词法降级、向量指纹和 checkpoint 隐私；无新修改时未重复这组测试。
- 固定真实 E5 tokenizer：长历史 + Redis 为 28 tokens、未超长，最终全部表达式含 Redis。
- 单独词法测量：中文术语/自然问句、英文自然问句、精确术语和代码标识符均命中。英文自然问句宽松回退也召回 worker 标识符相关行，说明 OR 可能增加噪声，未声称精确率已通过真实质量评测。
- 同义对照：同一资料 car=1、automobile=0；汽车=1、机动车=0。纯词法无共享词项时不支持同义替换，需 dense/真实质量评测单独判断；没有把这一限制标为已修复。
- 结论：D1 的检索和回归范围通过；阶段一完成 5/5。真实模型、答案事实校验、复杂语境及目标生产资格仍未验证，进入 Q2-01。
