# Q1-01：复核代码基线、复现材料与实施范围

- 状态：已完成（复核任务，无产品改动，无 Harness 规格）。
- 复核时间：2026-09-12；复核者环境：本机 Windows 工作区，未连接网络、未调用模型。
- 审查基线：`542d9167bc9e93294ccd7b5e17c23795c43bd41a`。

## 1. 代码基线是否漂移

- 当前 `HEAD` 仍为 `542d916`，与审查基线同一提交。
- 工作区仅有计划目录未跟踪（`plan-build/assistant-gap-closure/`、`plan-build/assistant-safety/reviews/`、`plan-build/site-functional-audit/`、`plan-build/site-functional-fixes/`）与 `plan-build/README.md` 修改；`apps/`、`packages/`、`scripts/`、`harness/` 无工作区改动。
- 结论：审查发现针对的代码状态没有变化，报告中的 R（已复现）项不需要重做归因，但失败测试必须重新实际执行后才能引用。

## 2. 材料定位（忽略目录，不随 clone 提供）

目录 `apps/api/data/qa-gap-audit-20260912/` 存在且完整：

- `replay.py` + `probe-results.json`：42 项探针，分布为 answer-validator 24、preflight 8、real-sqlite-fts 4、evidence-scan 3、estimator 1、query-to-fts 1、pinned-e5-tokenizer-to-fts 1；脚本自身屏蔽 socket，未调用供应商。
- `integration_replay.py` + `integration-results.json`：I01—I06；`legacy_replay.py` + `legacy-results.json`：I07（`20260907_0024` → `20260911_0030`，恢复日锁保留）。合计 7 项隔离接口诊断。
- 批次结果：`pytest-results-02.xml`、`pytest-results-03.xml`、`web-results.json`；`integration-work-01/`、`legacy-work-01/`、`pytest-work-02/`、`pytest-work-03/` 保存首次事实，重放不得覆写。

## 3. 本轮独立复现（未复用报告结论）

在 `apps/api` 下用项目虚拟环境实际执行，结果与报告一致：

| 核对项 | 命令/方法 | 结果 |
| --- | --- | --- |
| 三个失效测试 | `pytest tests/test_assistant_runtime_integrity.py tests/test_assistant_backup_restore.py` | 仍然 3 失败；前两个断言位于 `test_assistant_runtime_integrity.py:145`、`:218`，均为 `wait_until_started()` 返回 False |
| 失败归因（前两个） | 两个用例都以 `assistant_chat_max_input_tokens=4000` 建客户端 | 固定提示开销已超过 4000，模型未被调用即进入预算拒绝，竞态路径从未执行；与 G16 表格一致 |
| 失败归因（第三个） | `test_restore_previous_schema_creates_shared_budget_and_keeps_restore_lock` | `OperationalError: table admin_credentials already exists`，迁移重复建表；与 G16 表格一致 |
| G08 中文子串 | 真实内存 SQLite `assistant_chunk_fts` + `safe_match_query` | `向量检索`→0 命中，`全文检索`→0 命中，整句 `系统支持全文检索和向量检索`→1 命中 |
| G08 英文问句 | 同上 | `How does retry work?` 展开为四个 AND 前缀词项，`does` 不在正文 → 0 命中；单词 `retry`→1 命中 |
| G09 当前问题丢失 | `_retrieval_query("Redis", [长历史])` + `safe_match_query` | 拼接串保留 `Redis`，最终 MATCH 表达式为前 12 个词项，不含 `Redis` |
| 已修复项核实 | `validate_model_answer` 同句语境与 About 自述改写 | 同句“谣言：作者获得了图灵奖。”仍被拒；`我精通Python。`→`作者自述精通Python。` 通过；`项目使用FastAPI和SQLite。`→`项目采用FastAPI与SQLite。` 通过 |

## 4. 仍可复现 / 待核实

- 仍可复现：G16 三项测试前提漂移；G08 中英文自然问句词法召回不足；G09 最终 FTS 查询丢当前问题。
- 本轮未复核（不在本阶段范围）：G01—G07 的答案校验与误拒、G12—G14/G18 的运行与观测、G10/G11/G15 的能力边界；这些按各自阶段核对，不在此处下结论。
- 依赖环境的事实：本阶段所有结论来自本机虚拟环境与内存/临时 SQLite；不能替代 Q5/Q6 的真实模型、真实内容库与目标环境证据。

## 5. 实施边界与 SDD 划分

- Q1-02 只改两个测试文件的前置条件与旧库构造方式，不改产品代码；不得用“诊断里已通过”替换原测试重新通过。
- Q1-03 涉及 `apps/api/app/assistant_index/fts.py` 的写入切分与 `apps/api/app/search.py` 的查询构造，属索引/数据边界变化，需说明既有索引的重建或兼容路径。
- Q1-04 涉及 `apps/api/app/assistant/graph.py` 的检索查询构造，需对最终 MATCH 表达式断言，而不是只断言拼接字符串。
- Q1-02—Q1-04 都改动 `apps/` 下受管源码或测试，按 SDD 各自建立短生命周期 spec 并通过 task verify 关闭；Q1-05 验证与收尾。
- 生效限制沿用 D1：只恢复检索与回归有效性，不更换生成模型、不提高费用上限、不引入在线校验服务。

## 6. 对报告本身的独立意见

- 报告把“独立诊断通过”与“原测试已修复”区分为两件事，这个区分成立，本轮复现支持该判断。
- 报告 G08 结论写“向量分支可补救一部分”，本轮只能证明词法分支失效，未测量混合检索整体 Recall，不应据此推断线上召回已经不可用。
- 报告 G09 的“非 E5 分支或超长问题才发生”已被排除，本轮用字符串与 MATCH 断言再次确认丢词发生在 `safe_match_query` 的 12 词截断，而非 tokenizer 分支。

## 7. 结论

审查发现与当前状态一一对应，复现材料可定位，三个失败测试、G08 与 G09 均在本轮重新实测确认，实施边界与所需 SDD 任务已明确。进入 Q1-02。
