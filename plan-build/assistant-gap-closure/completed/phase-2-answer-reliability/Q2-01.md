# Q2-01 正反例与事实校验方案

- [诊断题集](../../evaluation/grounding-pairs.json)：原审查 24 项 + 本轮额外审阅 16 项；均为合成资料，额外集也不是独立人工标注基准。
- [可重放脚本](../../evaluation/compare_grounding.py)：从 `apps/api` 使用虚拟环境 Python 执行；主动禁止 socket，真实模型调用 0。
- [紧凑结果](Q2-01-evidence.json)：当前规则错放17/误拒8；完整句匹配错放2/误拒9；有限改写规范化错放2/误拒6。耗时为本机40例纯函数累计，不代表请求延迟。
- 独立判断：严格照抄并不能处理跨句撤回或来源冲突，还会损害合法改写；不能直接把候选替换现有在线校验。
- 选定实施：Q2-02 在保留引用、About自述、隐私与有限重试的前提下，增加普通陈述支持校验和限定语/撤回/同主体冲突保护；Q2-03 单独做数量和对象绑定；Q2-04 处理正例中的有限规范化与组合。Q2-05 技术文本结构与 Q2-06 讨论/指令分开处理，避免格式误拒混入语义结果。
- 残余：通用语义蕴含、任意改写、隐含指代和真实模型注入效果没有被本次规则比较证明。独立语义模型属于待评估候选，未加入在线服务、未产生外部费用；后续必须继续记录未支持范围。
- 完成范围仅为方案与离线比较，Q2-02—Q2-07 尚未完成，不能把本记录当作实现验收。
