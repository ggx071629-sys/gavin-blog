# S2-03 支持材料与失败策略

2026-09-11，局部验收通过。两种输出协议增加 supports，绑定所引当前正文，条目/长度有界；缺材料旧输出在校验处失败。保留原有重试上限和未知/截断处理。

store.write_terminal 在已有授权检查后同事务清除 parsed_json，覆盖成功、失败和恢复终止。公开、管理载荷及历史/checkpoint 不增加字段。模型替身携带实际材料；开发替身合并两段时分别保留引用。

局部检查：schema/缺失/错绑、公开终止成功与失败材料清理、DeepSeek 预算与图流程、管理试问隔离、开发替身合并均通过。源撤销及其他原有消费者进入 S2-04 正式精确验收。额外 schema/提示占用原预算，8000 上限的固定预算样例少选一个较大切片，未提高额度。

修复测试中已过时的“提示 descriptor 含 path”断言，恢复先前路径呈现边界，不将其计为新增产品行为。
