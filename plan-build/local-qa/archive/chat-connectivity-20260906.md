# 阶段 E 已执行的连接测试

日期：2026-09-06。仅归档已执行测试的事实；阶段 E 整体尚未完成。
读取条件：需要追溯 token 数、请求差异、HTTP 400 或测试结果边界时。
返回 [主计划中的当前任务](../build-qa.md#current-task)。

管理员填写配置并明确要求确认可调用后，本轮执行两次最小请求，均复用既有 `build_chat_model`，输出上限 256 tokens、超时 45 秒、自动重试与 tracing 关闭。只使用合成提示，没有发送博客正文或会话历史。以下事实来自当前任务的脱敏工具结果，不作为生产资格 manifest：

| 检查 | 结果 | 可得结论 |
| --- | --- | --- |
| 配置读取 | provider、endpoint、model、Key 均有效读取 | 原有 `.env` 及 Key 变量引用生效 |
| 项目严格结构化请求 | HTTP 400；错误涉及 `response_format` | 当前请求与接口存在兼容性问题；尚未确认供应商支持的完整格式范围 |
| 普通文本请求 | 成功，`finish_reason=stop`，返回模型与配置一致；输入 9、输出 2、合计 11 tokens | Key 和基本 LangChain 调用可用；该响应未精确满足“只回复 OK”的内容检查，不作为回答质量通过 |

普通请求在官方 DeepSeek 地址下显式关闭 thinking，以区分基本连通性与结构化输出问题；该成功不代表现有问答流程已经完成适配。失败请求未取得 usage，不能声称两次请求合计只消耗 11 tokens，实际费用尚未核实。全过程未输出 Key 或供应商原始响应，未修改已有客户端代码、未放宽结构化校验，也未启用页面/API 在线问答或运营 gate。

## 配置与清理记录

管理员在原有 `apps/api/.env` 填写 Key；顶部 `DEEPSEEK_API_KEY` 由 `GAVIN_ASSISTANT_CHAT_API_KEY` 引用，配置已成功读取。此前额外新增的本机探测、独立账本和空白 `.env.chat` 已按管理员要求撤销。当前继续复用已有 LangChain 和生产资格探测逻辑。本文件不保存 Key、端点配置值或原始响应。
