# R3 完成

- 同一路径v3签名ledger嵌入旧ledger原文本、摘要和结构；授权ID、累计上限、配置摘要与日预算独立登记，所有attempt保留。
- 同ID重放不增额度，旧ID拒绝；调用和金额均按跨授权累计数校验，新ID不能重置费用。
- sending/unknown/measured_fail需静止确认及原因后追加恢复事实；未知保守计费，状态和原金额未改，恢复不授权调用。
- 探测/授权/恢复共用独占run lock；新授权的探测结果必须使用新文件。费用授权重绑离线保留旧签名证据和completed_at。
- 专项9项通过，含真实lock冲突、签名篡改、次数/金额耗尽、三类恢复和无隐式网络；此前生产/阶段1相关38项通过。
- 未操作真实账本和费用授权；正式隔离验证在R4。
