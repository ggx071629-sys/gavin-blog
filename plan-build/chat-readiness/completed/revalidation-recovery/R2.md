# R2 完成

- v2签名证据独立绑定兼容性（含输出schema）；价格和授权内日预算只做本地校验，readiness仍绑定当前价格和预算并在变化后本地重签。
- v1离线转换要求原配置并输出到不同文件，保留原签名payload、字节摘要和completed_at。
- local_probe_recovery与assistant_real_development共23项通过；价格变更无新增调用，凭据/endpoint/token/timeout/schema漂移拒绝，阶段1年龄/生产边界保持。
- 原validator v1摘要兼容不变；原真实文件和账本未操作。R4会执行完整固定HEAD隔离验证。
