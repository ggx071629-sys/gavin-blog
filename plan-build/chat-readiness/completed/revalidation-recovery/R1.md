# R1 完成

- 已分类：兼容性摘要绑定凭据/endpoint/模型/token/context/timeout/输出schema；价格和日预算本地校验；ledger授权为累计调用和金额上限，独立于日预算。
- 旧v1必须在原配置下离线验证后输出到不同文件，保留原字节和完成时间，不推断未知配置。
- 同一账本新增唯一授权ID，保留旧快照和全部attempt；累计消耗永不重置，同ID重放不增额度。
- sending/unknown/measured_fail须静止确认和原因后追加恢复事实；原状态和费用不改，未知保守计费。额度耗尽必须显式累计新上限。
- spec 95e3194已提交，spec-lint通过。未授权真实付费，但不阻塞规则与隔离实现。
