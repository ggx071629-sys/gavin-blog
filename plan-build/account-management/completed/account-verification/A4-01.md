# A4-01：真实 HTTP 联调

- 完成：2026-09-11。`real HTTP password change recovery and revocation` 1 passed，39.1 秒，子 API/临时数据库与运行器清理通过。
- 数据库由 Alembic 升至 head；真实 FastAPI/SQLite、真实密码哈希与 Cookie/CSRF。浏览器只作 HTTP 传输代理，不合成认证响应；邮箱 sender 注入测试文件，不外发。
- 改密当前密码错误拒绝、错误验证码拒绝、正确改密后两个旧会话 401、新密码真实登录；匿名找回后旧会话 401、新密码再次登录；同用途发送配额在两次密码变更后仍有效。
- 复用 A3-04 三会话单个/其他/当前撤销结果；`tests/test_account_management.py` 15 项通过，覆盖迁移、终端恢复、用途/过期/错误限次/重复/并发/邮件失败/兼容等边界。
- 此处没有真实邮件投递结论，真实 SMTP 接受、收取和验证码闭环单独归 A4-02。
