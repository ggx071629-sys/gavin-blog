# A3-02：当前密码与邮箱验证码改密

- 完成：2026-09-11（北京时间）。复用 field/button/AdminDialog，发送与提交独立、冷却按服务器值、全设备退出单独确认。
- `password form verifies errors confirmation and success`：1 passed，21.9 秒，运行器正常退出。覆盖发送失败不假成功、重发冷却、确认密码不符、取消、验证码错误保留输入、请求字段与成功清空跳转。
- 类型检查通过。首轮与 Nuxt prepare 并行造成 app-manifest 解析失败，已改串行重跑通过；这属于本地执行竞争，不修改应用构建合同。
- 本项浏览器响应注入只验证 UI；真实 Web/API 与真实 SMTP 分别归 A4-01、A4-02，不能替代。
