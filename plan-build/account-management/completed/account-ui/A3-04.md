# A3-04：登录会话管理

- 完成：2026-09-11。独立加载/刷新、真实 UTC 转本地时间、当前标记，单个/其他/当前退出均有确认及失败状态。
- `sessions revoke selected others and current with real API`：1 passed，26.5 秒，正常清理。三个隔离浏览器真实登录；单个撤销仅对应会话 401，其他会话暂保持 200；退出其他全部 401 而当前 200；退出当前跳登录且 API 401。
- 凭据和数据库均为隔离测试数据，无真实发信。
