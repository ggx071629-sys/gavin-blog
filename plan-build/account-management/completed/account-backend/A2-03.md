# A2-03 · 改密与全会话撤销

- 接通 account 信息、发送改密码与密码修改接口；敏感字段有长度/额外字段校验，认证响应及错误 no-store。
- 写事务内复核当前会话、密码和挑战；更新 Argon2id、递增版本、失效所有挑战并删除全部会话；成功清 Cookie。
- `pytest tests/test_account_management.py`：6 passed，2.18s。改密缺 CSRF、错当前密码、错验证码均拒绝；两个独立客户端全部失效；重建应用后旧密码拒绝、新密码可登录。
- 结果：隔离接口验收通过；最终并发与静态检查在 A2-07 汇总。
