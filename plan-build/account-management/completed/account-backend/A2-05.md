# A2-05 · 会话管理

- Alembic 0028 增加独立公开会话 ID 和活动时间；升级撤销旧会话。认证短事务更新活动时间；登录清过期并限制最多 100 活跃会话。
- 接通列表、单个（含当前）及其他会话撤销，写事务内复核当前会话；列表不返回 token/hash/CSRF。
- `pytest tests/test_account_management.py tests/test_migrations.py`：17 passed，19.96s。双客户端检查时间/当前标记、CSRF、撤销目标、当前退出及完整迁移链。
- 结论：A2-05 验收通过；真实浏览器页面属于未进入的阶段三。
