# A2-07 · 后端契约与正式验收

- 生成 `packages/contracts/openapi.json` 与 `api-types.d.ts`；现有登录响应未改，新增账户页面消费留第三阶段。
- 完成配置示例、API/契约说明及持久决策，登记 11 个所选 case、35 个精确测试引用与独立影响计划。
- 修复回归暴露的 ORM 缓存竞态：重开认证事务强制 expire_all，在途 SMTP 不得激活重置前验证码；恢复先验证验证码再判断新旧密码相同，避免密码匹配探测。更新旧会话时间夹具与配置字段覆盖。
- 静态检查：变更 Python 文件 Ruff 通过，`mypy app` 对 117 个源码文件通过。
- 隔离回归：账号、原登录/写作鉴权、时间与运行配置检查 30 passed；新增旧密码并发登录/重置回归另 1 passed。
- 正式验证：干净提交 `ccdb4443455fd179be066e835c212917d42b3aa2` 执行 `python -m tools.harness verify 20260910-account-management`，11 cases / 35 exact refs，13 checks passed，35777.6 ms；Harness 临时隔离清理通过。机器证据：[manifest](../../../../harness/verification/evidence/20260910-account-management.json)。
- 原工作区无关 Web README/lockfile 改动及 SiteNavIcon 缺归属阻止直接验证；保留原样，在独立干净工作树完成正式快照验证。没有把无关改动提交到本任务。
- 结论：第二阶段 7/7 完成，按用户指令停止。真实 163 发送/收取未执行；第三阶段 UI、第四阶段联调与 spec 确定性关闭仍未开始。此证据证明后端提交，不证明真实投递或页面验收。
