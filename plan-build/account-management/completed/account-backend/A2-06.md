# A2-06 · 终端应急恢复

- 新增 `python -m app.reset_admin_password --database PATH --confirm-stopped`，要求已存在且已迁移 SQLite、停服备份确认；getpass 双次输入，不接收命令行密码。
- 复用密码策略、Argon2id 与原子全部会话/验证码失效，不依赖 SMTP 配置。
- `pytest tests/test_account_management.py`：9 passed，3.40s；直接运行 CLI main 的隔离测试验证无邮件配置、密码不回显、两个会话失效及新密码登录。
- 结论：A2-06 验收通过；未对日常使用数据库执行重置。
