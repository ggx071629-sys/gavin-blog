# A2-02 · 邮件挑战生命周期

- 新增 AccountChallenge/AccountAttempt 和 Alembic 0027；独立秘密 HMAC、用途/凭据版本绑定、持久发送与核验限额。
- SMTP SSL 验证证书，10 秒超时、固定收件人；先预留再发信，成功激活，失败不泄漏提供商异常、不退配额、不自动重试。
- 检查：`pytest tests/test_account_management.py`，5 passed，1.31s；覆盖生命周期、限次、用途、过期、重用、发送失败、跨服务重建配额、秘密轮换、TLS 适配。
- 曾发现新测试插入位置错误，修正后重跑通过。并发密码更新/在途发送集成由 A2-03/07 继续覆盖。
- 结论：隔离邮件替身验收通过；没有实际连接 163，不代表真实投递完成。
