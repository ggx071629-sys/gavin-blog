# O001：无限保存恢复修复

2026-09-12，阶段 6 交付后按用户追加要求完成。**持续无响应时无限“保存中”的恢复缺口已修复；原请求 #4950 挂起的传输触发原因仍未确定。** 本次有独立规格、失败反证和通过的浏览器回归，不改写原审查或六阶段 23/23 的历史记录。

## 定位依据

- 原记录只有单次 pending、截图和事后访问日志观察，缺少浏览器、代理、API 入口之间的关联 trace。已复核原任务的“新建→编辑→取消编辑→重复 slug 提交”序列。
- 原组件直接等待 `apiFetch`，只有请求结束才进入 `finally` 解除保存状态。受控扣住 POST 响应后，修复前真实 Chromium 页面在 18 秒断言期限结束时仍显示禁用的“保存中”，新增回归失败，证实界面恢复缺口。
- 访问日志需要修正解释：当前本地 Uvicorn 的 `h11_impl.py` 在 `http.response.start` 时记录访问日志。因此“没有 POST 访问日志”不足以证明“请求没有到达 API”；健康检查成功也不能排除写请求等待。没有证据将原触发归因于浏览器、代理或 API 的某一层。

## 已交付行为

[TaxonomyManager.vue](../../apps/web/components/TaxonomyManager.vue) 对栏目和标签共用的保存、删除及核对请求设置 **15 秒局部等待上限**。超时结束界面等待、中止客户端请求，保留表单并提示操作结果尚未确认。用户可点击“核对最新列表”，通过有界 GET 更新列表，之后自主决定是否重试；不自动重发写请求。组件离页后不接受迟到结果覆盖状态。

客户端中止不保证服务端回滚：请求可能已成功写入、但响应丢失。测试覆盖了这一情况，核对显示真实已存记录且不会清空输入或再次 POST。15 秒是该组件的交互等待预算；没有更改全站请求、代理、后端或数据结构。

## 验证与关闭

| 验证 | 结果与范围 |
| --- | --- |
| 修复前失败反证 | 请求持续无响应时，18 秒后按钮仍禁用；本地 trace 留存 |
| 正式 Harness | `74696da` 上 5 个精确 Playwright 引用全部通过；13 项检查全部通过 |
| 异常回归 | 栏目/标签创建悬挂；真实写入后响应丢失；核对 GET 超时与恢复；修改/删除悬挂后显式核对及操作 |
| 正常回归 | 栏目新增、修改、删除、持久化和关联保护；标签重复 slug 五轮真实 409 与按钮复位 |
| 原操作序列补充复核 | `cdc79a8` 上 1 条 Chromium 测试通过，栏目和标签的新建→编辑→取消→重复提交均真实返回 409，刷新后数据正确 |
| 静态检查 | 修改的组件与新增 E2E 文件 ESLint、Web typecheck 通过 |
| 环境收尾 | 正式 evidence 的 `cleanup_status=passed`；补充测试后 3100/8100 无监听，本任务隔离 worktree 已移除 |

规格基线提交 `a9d5df9`，实现提交 `74696da`，确定性关闭提交 `cdc79a8`。原始规格保留在 Git 历史；[Harness 归档](../../harness/specs/archive/20260912-taxonomy-pending-recovery.md) 与 [正式证据](../../harness/verification/evidence/20260912-taxonomy-pending-recovery.json) 已提交。证据 SHA-256：`dfe4a55a19a856ff2ab68d7321e4a0c295cbcc1458579b269d21ecbcfefe9725`。

本地反证、补充测试和运行索引位于 `.run/site-functional-fixes/20260912-o001/`，原始 trace、日志和临时数据不提交。仅本地提交，无推送或生产发布。测试证明故障恢复行为有效及原操作序列当前正常，不证明历史传输触发原因已定位；若原挂起再次出现，仍需采集关联 trace。

返回 [交付结果](RESULTS.md) · [总计划](PLAN.md)。[阶段 6 交付清单](delivery-manifest.json) 保留当时 O001 未解决的快照，当前后续状态以本报告为准。
