# 读书封面选择与校验：阶段主计划

## 总体进度

- 完成：4/4；总体状态：完成（阶段任务与 Harness 确定性关闭均完成）。
- 当前任务：无（本阶段已归档）；下一阶段见总入口。
- 阶段目标：让媒体库选择封面后的保存、发布及公开显示正常完成。
- 来源与范围：I001；[原问题报告](../site-functional-audit/ISSUES.md)。
- 有效限制：沿用站内媒体路径约定，不放宽到任意相对路径；Markdown 导入元数据未同步（剩余风险）。
- 执行边界：已实施、验证并归档；证据留在本地 `.run/site-functional-fixes/20260912-01/`。
- 顺序说明：按总入口顺序推进；F3-02 以 F3-01 的地址契约核对与 422 复现为前置。

## 任务清单

| 编号 | 任务名称 | 状态 | 关键前置 |
| --- | --- | --- | --- |
| F3-01 | 核对媒体地址契约并复现封面保存失败 | 已完成 | F1-01 隔离基线；I001 证据 |
| F3-02 | 统一封面选择、校验及保存读取行为 | 已完成 | F3-01；SDD 前置满足 |
| F3-03 | 补充本站媒体与既有封面输入回归 | 已完成 | F3-02；影响计划明确 |
| F3-04 | 真实浏览器复验选图至公开显示闭环 | 已完成 | F3-03 |

## 当前任务

**无 · 阶段 3 已完成并归档**

- 交付：I001 已修复——媒体库选中的站内封面可保存、刷新保持，发布后访客看到同一图片。
- 结论与证据索引见 `completed/phase-3-book-cover/F3-01.md` … `F3-04.md` 与 `.run/site-functional-fixes/20260912-01/`。
- Harness：已在阶段 6 通过 `verify 20260912-book-cover-media-url --close` 关闭；[归档](../../harness/specs/archive/20260912-book-cover-media-url.md) 与 [正式证据](../../harness/verification/evidence/20260912-book-cover-media-url.json)。
- 下一步：本轮已交付，当前结论与剩余限制见 [修复结果索引](RESULTS.md)。

## 简短验收说明

- 适用条件：[本阶段验收](DESIGN.md#i001)、[验证方式](DESIGN.md#implementation)；只引用，不扩充门槛。
- 实现或验收时再读设计相关章节；只有追溯具体事实时读取对应任务历史。
- 未开始＝未执行；进行中＝已启动；阻塞＝具体障碍；未验证＝证据不足；完成＝本任务交付满足条件。
- 技术失败、观察未知与等待前置分别记录；不得把未验证算通过或无理由改成阻塞。
- 产品修复完成须有适用回归证据；计划文档生成或替代路径可用不能代替修复完成。
- 未完成任务不归档；后续任务只保留清单行，轮到执行时才展开必要步骤。
- 原始附件留本地，归档只含紧凑事实与证据索引；维护语义见 [约定](DESIGN.md#maintenance)。

## 已完成事项

- 完成数量 4；归档位置 `completed/phase-3-book-cover/`。
- F3-01：媒体库返回 `/api/v1/media/{id}/webp`；前端 `type=url` 与服务端 `HttpUrl` 同时拒绝；既有头像已允许站内媒体路径。
- F3-02：`schemas.py` 增加 `SiteMediaAssetPath` 并用于书籍封面；写作台支持字段级文案覆盖；读书编辑器与新建页接受双形式封面。
- F3-03：新增 API 契约回归与 Web 闭环回归（API 用例修复前 422 失败）；`test_projects_books.py`+`test_profile.py` 21 与 Web 单元 159 通过。
- F3-04：真实 Chromium 复验“上传图片 → 媒体库使用 URL → 自动保存 → 刷新 → 发布 → 独立访客显示同一图片”闭环。
