# F4-03 · 补充三类型换行、无效输入及导出回归

状态：完成。

## 新增回归

`apps/api/tests/test_content_portability.py`：
- `test_markdown_import_treats_lf_and_crlf_alike`：三类内容（文章/项目/读书）各导入 LF 与 CRLF 同内容文件，断言标题/摘要一致、正文一致且不含 `\r`、状态均为 draft、公开列表为空。
- `test_markdown_import_keeps_front_matter_validation_with_crlf`：CRLF 下缺 front matter、未闭合、YAML 别名、非法 YAML、非 UTF-8 均 422；合法 CRLF 仍 200。

## 反证

临时移除归一化后运行：上述 4 条全部失败（`front matter is required`）。

## 关联回归

- `pytest tests/test_content_portability.py tests/test_taxonomy.py` 20 通过（含既有导入导出、重复 slug 409、批次与复杂度限制）。
- 浏览器用例 `ops markdown import accepts CRLF drafts` 通过；同文件既有 `ops content` 通过。

证据：`.run/site-functional-fixes/20260912-02/verification.json`
