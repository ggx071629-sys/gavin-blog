# F4-02 · 实施换行兼容并保持逐文件错误反馈

状态：完成。

## 变更

- `apps/api/app/routes/admin_content.py`：`parse_markdown` 在 UTF-8 解码后执行 `replace("\r\n", "\n").replace("\r", "\n")`，再做原有的 front matter 与正文解析。
- `apps/api/README.md`：记录解析前统一换行，并明确换行兼容不放宽 front matter、YAML 与编码校验。

## 关键决定

- 不新增 ZIP 导入、不扩展编码或格式、不改批次与字节上限。
- 仍使用抛错而非吞错：缺少/未闭合 front matter、别名、非法 YAML、非 UTF-8 继续 422。
