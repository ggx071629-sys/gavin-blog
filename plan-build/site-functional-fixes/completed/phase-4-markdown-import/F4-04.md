# F4-04 · 真实浏览器复验导入结果与草稿隔离

状态：完成。

## 复验

真实 Chromium + 真实隔离 API：在 `/admin/content` 上传由 LF 文档整体转成 CRLF 的 Markdown 文件，界面显示「已导入 1 项草稿。」；通过编辑链接进入草稿，正文为「第一行/第二行」且不含 CR；`/api/v1/articles` 公开列表不含该 slug；清理后删除草稿。

## I002 / S01 交接结论

- 已修复：CRLF（及单独 CR）Markdown 不再被误判缺少 front matter，且在浏览器与 API 两侧均得到一致结果。
- 已验证：新增回归 + 反证 + 既有导入/导出与栏目标签回归。
- 未验证/剩余风险：存储正文会随归一化改写为 LF，导出与公开读取按 LF 往返（本次未改变导出实现）；不涉及 ZIP 导入（产品本就不提供）。
- Harness 规格：`20260912-markdown-line-endings` 已登记；逐任务 `verify --close` 未执行（原因见运行记录）。

证据索引：`.run/site-functional-fixes/20260912-02/`
