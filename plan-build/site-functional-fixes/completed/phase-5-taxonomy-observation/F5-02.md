# F5-02 · 按定位结论定向处理并完成适用回归

状态：完成（无产品改动）。

## 处置

- 定位结论为“未复现、根因未知”，没有证据支持产品改动，因此不修改 `taxonomy` 页面或 API，也不新增超时/重试。
- 适用回归：在既有宿主 `apps/web/tests/e2e/resource-ops.spec.ts` 新增
  `ops duplicate tag slug reports a conflict without a stuck pending state`：
  连续 5 次重复 slug 提交，逐次断言收到 409、按钮回到「新建标签」、提示为保存失败，并断言最大往返时间小于 10s。

## 结果

- 该用例与既有 `ops taxonomy` 回归一起通过，守护“重复 slug 必须给出冲突反馈且不卡在保存中”。
- 未引入任何遥测或重试策略。
