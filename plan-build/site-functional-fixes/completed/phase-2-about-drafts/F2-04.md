# F2-04 · 真实浏览器复验保存发布并交接结果

状态：完成。

## 复验

- 真实 Chromium + 真实隔离 API：受控延迟的真实 PATCH 放行后，未提交标签输入保留；提交后正常清空。
- 保存、刷新、预览、发布与草稿公开隔离由既有 about 回归（草稿预览、十项上限、发布）覆盖并全部通过。

## I003 交接结论

- 已修复：无关字段自动保存不再静默删除正在输入的标签。
- 已验证：新增回归 + 反证 + 关联回归。
- 未验证/剩余风险：草稿按领域索引跟随，若未来改为服务端排序需重新核对；未验证其它并发编辑场景。
- Harness 规格：`20260912-about-draft-preserve` 已登记；逐任务 `verify --close` 未执行（原因见运行记录）。

证据索引：`.run/site-functional-fixes/20260912-01/`
