# F2-03 · 补充保存期间输入及领域操作回归

状态：完成。

## 新增回归

`apps/web/tests/e2e/about-admin.spec.ts`：
- `about autosave keeps an unsubmitted tag draft until it is added`：暂缓真实 PATCH → 输入未提交标签 → 放行 → 断言输入仍保留、保存状态为「工作副本已保存」；提交标签后输入清空。
- `about tag drafts follow their capability across reorder and removal`：草稿随领域上移，删除后草稿与领域保持对齐。

## 反证

临时回退修复代码后运行：两条均失败，证明回归能捕获原缺陷。

## 关联回归

`about-admin.spec.ts` 相关 5 条（草稿发布隔离、十项上限与增删、studio 关于页编辑器）全部通过；Web 单元 159 通过。

证据：`.run/site-functional-fixes/20260912-01/verification.json`
