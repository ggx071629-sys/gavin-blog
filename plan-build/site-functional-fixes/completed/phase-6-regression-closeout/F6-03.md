# F6-03 · 完成适用 Harness 关闭及隔离环境收尾

状态：完成；日期：2026-09-12。

四个规格均由 `python -m tools.harness verify <task> --close` 执行所选检查，并在同进程消费认证 evidence 后确定性关闭。原 active spec 保留于 Git 历史；压缩归档、exact evidence SHA-256 与 spec.completed 事件均已生成并提交。对应路径及提交见 [交付清单](../../delivery-manifest.json)。未手工伪造关闭或跳过门禁。

关闭准备中修正的实际缺口：

- 未提交规格不能 spec-lint/close：先提交规格再验证，既有实现移交事实保留，不伪称此前已完成正式关闭。
- 恢复文件曾因 Git 索引缓存被报告源码漂移；确认 git diff 无内容差异后刷新索引，再运行。该轮前置失败未执行产品测试。
- 封面 API README 缺失契约声明，已补齐；影响计划把直接消费 schema 的读书路由错误标为不受影响，已改为 affected 并补齐下游项目页面/配置判断，从源码选择 API 回归。
- Markdown 的同一个 case 被两条 AC 引用，但 e2e gate 只映射 AC-1；已补齐 AC-2 映射，测试集合不扩张。

失败前置与未运行记录保留在本地运行 README 与日志；最终认证 evidence 均 passed。没有应用任何 Evolution 提案。

四轮 isolation cleanup 均 passed；本轮临时 worktree 不再存在。最终 3100/8100/3200/8200 无监听，测试拦截随上下文释放；补充专属 SQLite/媒体与初始改动备份保留在忽略目录，未处理其他任务服务、worktree 或真实数据。运行检查见 `.run/site-functional-fixes/20260912-03/cleanup.json`。

O001 保持未解决，无对应修复 spec；其他任务的 active spec 不关闭。下一步：F6-04 更新总入口并交付归档。
