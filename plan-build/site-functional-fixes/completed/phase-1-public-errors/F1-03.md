# F1-03 · 补充精确自动回归并验证受影响消费者

状态：完成。

## 新增回归

`apps/web/tests/e2e/public-experience.spec.ts`：
- 首页→文章 503：目标路由渲染错误页（H1「服务暂时不可用」、重试按钮可见），首页 H1 消失；解除注入后重试恢复 `articles-results`。
- 关于→读书 503：错误页站内入口「浏览文章」清除错误并进入 `/articles`。
- 搜索后续失败：保留关键词（`/search?q=`），重试后恢复结果。

## 反证

临时回退修复代码后运行：前两条失败（旧页面残留），第三条通过——与审查“搜索后续失败已正确显示错误页”的结论一致。

## 关联回归

- `tests/failure/failure-state.spec.ts` 8/8（SSR 503、taxonomy 退化、404/500 区分、管理员页失败）
- `tests/e2e/articles-index.spec.ts` 4/4、`tests/e2e/books-index.spec.ts` 4/4、`tests/e2e/search-states.spec.ts` 2/2
- Web 单元 32 文件 159 通过

证据：`.run/site-functional-fixes/20260912-01/verification.json`
