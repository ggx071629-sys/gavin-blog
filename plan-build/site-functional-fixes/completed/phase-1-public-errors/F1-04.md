# F1-04 · 真实浏览器复验并交接 I004 结果

状态：完成。

## 复验

- 真实 Chromium + 真实隔离 API：F1-03 的三条用例全部通过；解除受控 503 后重试恢复目标内容；错误页站内入口可达目标路由。
- 服务端直接访问：`failure-state` 的公开页 503 与详情 404/500 用例保持通过，说明客户端修复未改变服务端语义。

## I004 交接结论

- 已修复：首次客户端加载失败不再残留旧页面内容，且提供操作内恢复入口。
- 已验证：新回归 + 关联回归 + 单元套件。
- 未验证/剩余风险：其余 `useApiFailure` 消费者（管理页、助手等）只继承共享修正，未逐一复验；错误页站内恢复依赖 `clearError` 行为。
- Harness 规格：`20260912-public-error-recovery` 已登记（cases/影响计划/契约）；逐任务 `verify --close` 未执行，原因见运行记录“未完成/限制”。

证据索引：`.run/site-functional-fixes/20260912-01/`
