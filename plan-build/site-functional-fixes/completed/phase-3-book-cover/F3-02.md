# F3-02 · 统一封面选择、校验及保存读取行为

状态：完成。

## 变更

- `apps/api/app/schemas.py`：抽出 `SiteMediaAssetPath`（`/api/v1/media/{id}/webp|avif`），`ProfileMediaAvatarPath` 复用同一约束；`BookNoteCreate/Update.cover_url` 改为 `HttpUrl | SiteMediaAssetPath | None`。
- `apps/web/components/WritingWorkspace.vue`：字段可用 `data-writing-error` 覆盖通用错误文案，未标注字段行为不变。
- `apps/web/components/BookNoteEditor.vue`、`apps/web/pages/admin/books/new.vue`：封面输入改为 `type="text"` + 双形式 pattern + 字段级文案，并加 `data-testid`。

## 关键决定

- 保存与读取保持原样（`str()`/直接 `<img :src>`），站内相对地址同源可用，不新增迁移。
- 显式清空封面仍由既有 `cover_url: null` 分支处理。
