# F3-03 · 补充本站媒体与既有封面输入回归

状态：完成。

## 新增回归

- `apps/api/tests/test_projects_books.py::test_book_cover_accepts_site_media_path_and_keeps_http_urls`：站内路径创建/读取/更新；http(s) 保留；`/api/v1/media/0/webp`、`png`、查询串、`/etc/passwd`、`//cdn…`、`ftp://`、`javascript:` 均 422；显式清空成功。
- `apps/web/tests/e2e/book-cover.spec.ts`：真实上传图片 → 媒体库「使用 URL」→ 自动保存 → 刷新保持 → 非法地址仍被拒并给出字段文案 → 未标注字段（Slug）沿用原分类文案 → 发布 → 独立访客上下文加载同一图片（`naturalWidth > 0`）。

## 反证

临时回退 `schemas.py` 后运行 API 用例：站内封面地址返回 422 而失败。

## 关联回归

`apps/api` pytest `test_projects_books.py` + `test_profile.py` 21 通过（既有 http(s) 与头像契约未回归）；Web 单元 159 通过。

证据：`.run/site-functional-fixes/20260912-01/verification.json`
