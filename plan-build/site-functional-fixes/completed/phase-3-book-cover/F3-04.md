# F3-04 · 真实浏览器复验选图至公开显示闭环

状态：完成。

## 复验

真实 Chromium + 真实隔离 API，按 P3-03 的路径复验：书籍封面 → 选择已有媒体 → 使用 URL → 自动保存 → 刷新 → 发布 → 独立访客上下文显示同一图片。修复前该流程在表单校验处被拒，现已完成闭环。

## I001 交接结论

- 已修复：媒体库选封面可保存、刷新保持、发布后公开显示同一图片。
- 已验证：API 契约回归、Web 闭环回归、反证、关联回归。
- 未验证/剩余风险：Markdown 导入元数据仍只接受 http(s)，站内封面经 Markdown 往返导入会 422；未扩展 ZIP 或编码。
- Harness 规格：`20260912-book-cover-media-url` 已登记（含 `apps/api/README.md` 文档目标）；逐任务 `verify --close` 未执行（原因见运行记录）。

证据索引：`.run/site-functional-fixes/20260912-01/`
